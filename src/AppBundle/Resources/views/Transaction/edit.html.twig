{% extends 'base.html.twig' %}

{% block body %}

        <h1 class="page-header">{{"%entity% edit"|trans({"%entity%":"Transaction"})}}</h1>

        <form class="form-horizontal" action="{{ path('transactions_edit', { 'id': entity.id }) }}" method="post" enctype="multipart/form-data" >
            <input type="hidden" name="_method" value="PUT" />
            {{ form_widget(edit_form) }}
        <div class="record_actions col-lg-10">
            
            <button class="btn btn-default" type="submit">
                <i class="glyphicon glyphicon-pencil"></i>
                {{ 'sauvgarder'|trans({})|capitalize }}
            </button>

            <div class="btn-group">
            <a class="btn btn-default" href="{{ path('transactions') }}">
                <i class="glyphicon glyphicon-th-list"></i>
                {{ 'retour à la liste'|trans({})|capitalize }}
            </a>

            </div>

        </div>
        </form>
        

{% endblock %}
